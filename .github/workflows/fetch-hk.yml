name: Fetch Stock Data

on:
  schedule:
    - cron: '0 10 * * *'  # 北京时间 18:00 = UTC 10:00
  workflow_dispatch:
    inputs:
      init:
        description: 'Initial full fetch'
        required: false
        default: 'false'

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pandas requests tushare

      - name: Run stock data fetch (tushare)
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: |
          python scripts/fetch_data.py --source tushare --init ${{ github.event.inputs.init }}

      - name: Commit and Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || git commit -m "chore: update stock data (tushare)"
          git push